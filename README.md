# Ghostquery
GhostQuery is a lightweight, terminal-based search query constructor designed to help users build advanced Google search queries using supported search operators. It provides an interactive command-line interface with OSINT-inspired terminal styling and requires no external dependencies.
